<Page x:Class="Khakimova.Pages.UsersTabPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:Khakimova.Pages"
      Title="Users Table"
      Background="{DynamicResource ContentBackground}">

    <Page.Resources>
        <local:StringToImageConverter x:Key="StringToImageConverter"/>

        <!-- Стиль для кнопок -->
        <Style x:Key="BlueButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource LoginButtonBackground}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="10,5"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource LoginButtonHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource LoginButtonPressed}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для кнопки редактирования в DataGrid -->
        <Style x:Key="EditButtonStyle" TargetType="Button" BasedOn="{StaticResource BlueButtonStyle}">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Width" Value="110"/>
        </Style>

        <!-- Стиль для DataGrid -->
        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="{DynamicResource CardBackground}"/>
            <Setter Property="AlternatingRowBackground" Value="{DynamicResource HeaderBackground}"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource CardBorder}"/>
            <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource CardBorder}"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="CanUserResizeColumns" Value="True"/>
            <Setter Property="CanUserSortColumns" Value="True"/>
            <Setter Property="Foreground" Value="{DynamicResource CardText}"/>
        </Style>

        <!-- Стиль для заголовков столбцов DataGrid -->
        <Style x:Key="DataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{DynamicResource LoginButtonBackground}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="{DynamicResource CardBorder}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <!-- Стиль для ячеек DataGrid -->
        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource CardText}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource CardBorder}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <!-- Стиль для строк DataGrid -->
        <Style x:Key="DataGridRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource CardText}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource CardBorder}"/>
        </Style>

        <!-- Стиль для границы фото -->
        <Style x:Key="PhotoBorderStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="{DynamicResource CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{DynamicResource HeaderBackground}"/>
            <Setter Property="CornerRadius" Value="4"/>
        </Style>

        <!-- Стиль для нижней панели -->
        <Style x:Key="BottomPanelStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource HeaderBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
        </Style>

        <!-- Стиль для текста ролей -->
        <Style x:Key="RoleTextStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource CardText}"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Таблица пользователей -->
        <Border Grid.Row="0" 
                Background="{DynamicResource CardBackground}" 
                Margin="10" 
                BorderBrush="{DynamicResource CardBorder}" 
                BorderThickness="1"
                CornerRadius="8">
            <DataGrid x:Name="DataGridUser" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      Style="{StaticResource DataGridStyle}"
                      ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                      CellStyle="{StaticResource DataGridCellStyle}"
                      RowStyle="{StaticResource DataGridRowStyle}">
                <DataGrid.Columns>
                    <!-- Убрана колонка ID -->
                    <DataGridTextColumn Header="Логин" Binding="{Binding Login}" Width="150" IsReadOnly="True"/>
                    <!-- Убрана колонка пароля -->
                    <DataGridTextColumn Header="Роль" Binding="{Binding Role}" Width="120" IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource RoleTextStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Role}" Value="Admin">
                                        <Setter Property="Foreground" Value="#E74C3C"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Role}" Value="User">
                                        <Setter Property="Foreground" Value="#27AE60"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="ФИО" Binding="{Binding FIO}" Width="250" IsReadOnly="True"/>

                    <!-- Колонка для отображения фотографии -->
                    <DataGridTemplateColumn Header="Фото" Width="120" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <Border Style="{StaticResource PhotoBorderStyle}" 
                                            Width="50" Height="50">
                                        <Image Source="{Binding Photo, Converter={StaticResource StringToImageConverter}}" 
                                               Stretch="UniformToFill" 
                                               Width="48" Height="48"/>
                                    </Border>
                                    <!-- Убрано отображение имени файла фото -->
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Действия" Width="130" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="✏️ Редактировать" 
                                        ToolTip="Редактировать данные пользователя" 
                                        Click="ButtonEdit_Click"
                                        Style="{StaticResource EditButtonStyle}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Панель кнопок -->
        <Border Grid.Row="1" 
                Style="{StaticResource BottomPanelStyle}"
                Margin="10,5,10,10" 
                Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="ButtonAdd" 
                        Content="➕ Добавить пользователя" 
                        ToolTip="Добавить нового пользователя" 
                        Click="ButtonAdd_Click"
                        Style="{StaticResource BlueButtonStyle}" 
                        Width="190" 
                        Margin="5"/>
                <Button x:Name="ButtonDel" 
                        Content="🗑️ Удалить выбранных" 
                        ToolTip="Удалить выбранных пользователей" 
                        Click="ButtonDel_Click"
                        Style="{StaticResource BlueButtonStyle}" 
                        Width="174" 
                        Margin="5"/>
                <Button x:Name="ButtonBack" 
                        Content="← Назад" 
                        ToolTip="Вернуться на предыдущую страницу" 
                        Click="ButtonBack_Click"
                        Style="{StaticResource BlueButtonStyle}" 
                        Width="100" 
                        Margin="5"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>