<Page x:Class="Khakimova.Pages.UserPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="User Page">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель фильтров -->
        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <TextBlock Text="Введите ФИО для поиска:" FontSize="9" VerticalAlignment="Center"/>
            <TextBox x:Name="fioFilterTextBox" Margin="7,7,0,7" MinWidth="100" TextChanged="fioFilterTextBox_TextChanged"/>

            <TextBlock Text="Сортировка по ФИО:" Margin="10,0,0,0" FontSize="9" VerticalAlignment="Center"/>
            <ComboBox SelectionChanged="sortComboBox_SelectionChanged" 
                      Margin="7,0,0,0" 
                      x:Name="sortComboBox" 
                      SelectedIndex="0" 
                      VerticalContentAlignment="Center" 
                      VerticalAlignment="Center" 
                      Width="120">
                <ComboBoxItem Content="По возрастанию"/>
                <ComboBoxItem Content="По убыванию"/>
            </ComboBox>

            <CheckBox IsChecked="False" 
                      Checked="onlyAdminCheckBox_Checked" 
                      Unchecked="onlyAdminCheckBox_Unchecked" 
                      Content="Только администраторы" 
                      x:Name="onlyAdminCheckBox" 
                      VerticalContentAlignment="Center" 
                      Margin="10,0,0,0"/>

            <Button x:Name="clearFiltersButton" 
                    Content="Очистить фильтр" 
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Center" 
                    Padding="5,8,5,4" 
                    Height="34" 
                    FontSize="10" 
                    Click="clearFiltersButton_Click_1"
                    Margin="10,0,0,0"/>
        </WrapPanel>

        <!-- Список пользователей -->
        <ListView Name="ListUser" 
                  Grid.Row="1" 
                  HorizontalContentAlignment="Center" 
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  Margin="10">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="10" Width="220">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="200"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>

                        <!-- ФИО -->
                        <TextBlock VerticalAlignment="Center" 
                                   TextAlignment="Center" 
                                   Width="200" 
                                   Grid.Row="0" 
                                   TextWrapping="Wrap" 
                                   Text="{Binding FIO}" 
                                   HorizontalAlignment="Center" 
                                   FontSize="16" 
                                   FontWeight="Bold"
                                   Margin="0,5,0,5"/>

                        <!-- Фотография -->
                        <Image Width="200" 
                               Grid.Row="1" 
                               Stretch="UniformToFill" 
                               HorizontalAlignment="Center">
                            <Image.Source>
                                <Binding Path="Photo">
                                    <Binding.TargetNullValue>
                                        <!-- Заглушка для изображения -->
                                        <BitmapImage UriSource="/Images/default.png"/>
                                    </Binding.TargetNullValue>
                                </Binding>
                            </Image.Source>
                        </Image>

                        <!-- Роль -->
                        <TextBlock VerticalAlignment="Center" 
                                   TextAlignment="Center" 
                                   Width="200" 
                                   Grid.Row="2" 
                                   HorizontalAlignment="Center" 
                                   FontSize="14" 
                                   Text="{Binding Role}"
                                   Margin="0,5,0,5"/>

                        <!-- Логин -->
                        <TextBlock VerticalAlignment="Center" 
                                   TextAlignment="Center" 
                                   Width="200" 
                                   Grid.Row="3" 
                                   HorizontalAlignment="Center" 
                                   FontSize="12" 
                                   Text="{Binding Login}"
                                   Foreground="Gray"
                                   Margin="0,2,0,5"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>